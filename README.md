# python-git-assignment
Домашнее задание по Python и Git

Простой калькулятор на Python для демонстрации работы с Git.

# Функции
`add(a, b)` - сложение двух чисел
`subtract(a, b)` - вычитание
`multiply(a, b)` - умножение
`divide(a, b)` - деление

# Использование

python
from calculator import add

result = add(5, 3)
print(result)  # 8
